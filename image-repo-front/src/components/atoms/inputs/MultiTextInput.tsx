import { styled } from "styled-components";
import InputProps from "../../../types/input-props.type";

const StyledMultiTextInput = styled.div`

`;

const MultiTextInput: React.FunctionComponent<InputProps> = (props) => {
    return (
        <StyledMultiTextInput>

        </StyledMultiTextInput>
    );
}

export default MultiTextInput;