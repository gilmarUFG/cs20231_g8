import { styled } from "styled-components";

type AppGridProps = {}

const AppGrid: React.FunctionComponent<AppGridProps> = (props) => {
    return (<></>);
}

const StyledAppGrid = styled(AppGrid)`

`;

export default StyledAppGrid;