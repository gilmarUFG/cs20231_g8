type ImagePageProps = {
    $userImage?: boolean;
}

const ImagePage: React.FunctionComponent<ImagePageProps> = (props) => {
    return <p>É pra ser a página de exibição de imagem</p>
}

export default ImagePage;